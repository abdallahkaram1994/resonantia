The two error_* files are real responses to requests sent with fake credentials (no third-party
content). The games are invented, in IGDB's response shape as verified against the live API.
Real IGDB records are not stored here because the repository is public.
