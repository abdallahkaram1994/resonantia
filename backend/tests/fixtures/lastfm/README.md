Invented records in Last.fm's JSON response shape, following its API documentation (including the
quirks that a single result arrives as an object, not a list, and that empty tags arrive as an
empty string). error_invalid_key.json is a real response to a request sent with a fake API key (HTTP 403).
The other files are not yet recorded from the live API. Real Last.fm data is not stored here.
